# API 模块规范

## 核心原则

- 所有接口统一通过 api 目录管理
- 不在页面中直接写 axios 请求
- 接口类型必须完整
- 接口命名保持统一

## Mock

- 新增接口时同步补充 mock
- mock 数据结构必须与真实接口一致

## 错误处理

- 统一走 request 层错误处理
- 不在每个页面重复 toast

## 修改原则

- 优先复用已有接口封装
- 不重复创建 request 实例
