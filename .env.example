# Application
VITE_APP_TITLE=PixSpace

# API Base URL
VITE_APP_API_BASE_URL=http://localhost:3000/api

# Dev only: allow mock login fallback when auth API is unavailable
VITE_ALLOW_MOCK_AUTH=false

# Data source switch for pages (mock/api)
VITE_USE_MOCK_DATA=true
